policy:
  name: IntentGenerationPolicy
  role: forward_generation
  audience: llm
  description: >
    该策略描述如何把事件转换为意向草稿。
    目前仅保留 speak 行为和 tags 检索。

output:
  - kind: speak
  - payload: {text}
  - draft_text + retrieval_tags
  - confidence/motivation/urgency
