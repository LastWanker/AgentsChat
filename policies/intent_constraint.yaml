version: 0.1
policy:
  name: IntentConstraintPolicy
  role: forward_constraint
  audience: system

kinds:

  speak:
    allow: true
    require:
      fields:
        - payload.text
    forbid:
      - "is_empty(get(intention.payload, 'text'))"
