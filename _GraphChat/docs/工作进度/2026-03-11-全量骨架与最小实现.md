# 工作进度（2026-03-11）

## 1. 本次目标

按 `_GraphChat/docs` 设计稿一次性搭建全量骨架与最小实现，不只做 Phase 0~1。

## 2. 已完成内容

## 2.1 目录与工程化

- 新建 `_GraphChat/pyproject.toml`
- 新建 `_GraphChat/src/graphchat` 分层目录
- 新建 `_GraphChat/run_demo.py` 与 `_GraphChat/README.md`

## 2.2 Domain 层

- `ActionSpec` 与默认动作注册表（含 `vote_decision`、`dissolve_group`、`maintain_board`）
- `Event` / `BoardItem` 模型
- `citation_policy` / `scope_policy`

## 2.3 Application 层

- `WorldState` / `AgentState`
- 守卫节点：`schema_guard`、`action_registry_guard`、`citation_guard`、`idempotency_guard`
- 主图：`world_graph`、`agent_graph`
- 子图：`silent_loop_subgraph`、`retrieval_subgraph`、`board_subgraph`、`governance_subgraph`
- 运行时：`GraphChatRuntime`

## 2.4 Infrastructure 层

- 持久化：`JsonlEventStore`、`BoardStore`、`InMemory checkpointer`
- 检索：`TagInvertedIndex`、`VectorIndex`
- 模型占位：`PlaceholderModelProvider`

## 2.5 Interfaces 层

- CLI：`graphchat.interfaces.cli.main`
- API 占位：`GraphChatAPI`
- UI 输入适配：`InputAdapter`（`chunk + commit`）

## 2.6 官方能力贴合点

- `StateGraph` 建图
- `Send(...)` 并发分发
- `Command(...)` 节点控制
- `interrupt(...)` 中断机制
- `compile(checkpointer=...)` 持久化
- 子图以“已编译子图节点”挂载

## 3. 运行验证

命令：

```powershell
python _GraphChat/run_demo.py
```

结果：

- 运行成功，返回多 Agent 输出事件（示例为 `vote_decision`）。
- 事件写入 `_GraphChat/data/sessions/<session_id>/events.jsonl`。

## 4. 本次修复记录

- 修复 `WorldGraph` 条件路由写法，确保 `Send` 分发分支实际执行。
- 修复 `idempotency_guard` 与 `emit_event` 的幂等冲突（避免提前占位导致误跳过）。
- 修复 `collect_agent_outputs` 聚合重复追加问题。

## 5. 待拓展（已在代码注释标注）

- checkpointer 从 InMemory 切换到 SQLite/Postgres。
- 检索子图接入真实 Web/File/Peer 通道。
- 模型层替换为 LangChain 结构化输出链或 AgentExecutor。
- 治理子图扩展转移群主、复杂投票策略等。
- API 层接入 FastAPI，UI 层接入实时流式展示。

