# 工作进度（2026-03-11）- P0稳定性闭环完成与P1能力增强首轮

## 1. 对照基线文档

本次实现对照并落实了以下文档要求：

- `docs/下一步工作计划/2026-03-11-下一步工作计划.md`
- `docs/可落地方案/03-功能层落地方案.md`
- `docs/可落地方案/04-实施计划与验收标准.md`
- `docs/可落地方案/99-官方API对照与约束.md`

## 2. P0完成标记（补记）

已完成并通过自动化验证：

- P0.1 checkpointer：运行时支持 SQLite/Memory 后端，默认 `auto`。
- P0.2 schema 强校验：事件与动作载荷做 Pydantic 结构化校验。
- P0.3 测试基线：状态机、引用守卫、scope、中断恢复测试已落地。

## 3. P1首轮实现（本次新增）

## 3.1 P1.1 RetrievalSubgraph 多通道可开关

实现：

- 新增 `infrastructure/retrieval/channels.py` 通道抽象与默认实现：
  - `world_history`
  - `ask_peer`
  - `file_search`（Provider 预留）
  - `web_search`（Provider 预留）
- 新增 `infrastructure/retrieval/fusion.py` 规则融合评分：
  - `semantic_sim/source_weight/freshness/dependency_graph/consensus`
  - 固定权重计算 `value_score`
- 升级 `application/graphs/subgraphs/retrieval_subgraph.py`：
  - 通道选择支持开关
  - 输出 `retrieval_trace`（通道命中、候选数量、focus/support）
  - 无候选时输出 `reroute_hint`

可扩展位（代码内中文注释已标注）：

- `SearchProvider` 可替换真实 Web/File 检索客户端，无需改主图。

## 3.2 P1.2 模型层切换到 LangChain 结构化输出路径

实现：

- 重写 `infrastructure/llm/model_provider.py`
  - `LangChainStructuredModelProvider`：`chat model + with_structured_output(PlanOutput)`
  - `RuleBasedModelProvider`：稳定兜底
  - `build_model_provider(...)`：环境驱动自动选择
- 运行时 `GraphChatRuntime` 默认走 `build_model_provider`，兼容手工注入 provider。
- `agent_nodes.plan_action_node` 增强模型输出防御（非dict/非dict payload 自动降级）。

环境约定：

- `GRAPHCHAT_MODEL`
- `GRAPHCHAT_MODEL_PROVIDER`

未配置时自动使用规则模型，保证开箱可运行。

## 3.3 P1.3 BoardSubgraph 功能补全

实现：

- `domain/models/board_item.py` 新增字段：
  - `priority`
  - `due_at`
  - `archived`
  - `deleted`
- `infrastructure/persistence/board_store.py` 增强：
  - `list/get/upsert/update`
  - `archive_item`
  - `delete_item`（软删/硬删）
  - `gc_items`
- `application/graphs/subgraphs/board_subgraph.py` 升级：
  - `upsert/delete/archive/set_priority/set_due`
  - `board_gc` 节点（子图结构已预留可扩展）
  - `board_operation` 写回 `action_payload` 便于事件审计链追踪

## 3.4 可视化元数据同步

- 更新 `application/graphs/graph_metadata.py`：
  - 检索子图状态字段补齐（含 `retrieval_trace`）
  - 任务板子图新增 `board_gc` 元数据
- 重新导出 `docs/graphs/graph_bundle.json` 和 `docs/graphs/graph_viewer.html`。

## 4. 测试与验证

新增测试：

- `tests/unit/test_retrieval_subgraph.py`
- `tests/unit/test_board_subgraph.py`
- `tests/unit/test_model_provider.py`

回归结果：

```powershell
pytest -c _GraphChat/pytest.ini _GraphChat/tests
```

- `12 passed`

## 5. 当前仍待完成（进入下一轮）

- Web/File/Peer 通道接入真实外部检索实现（当前为低耦合占位 Provider）。
- `citation_guard` 失败后的策略改道细化（`ask_peer/web_search/defer` 的策略化选择）。
- 任务板“操作事件”拆分为独立事件类型（当前先写入 action payload 审计链）。
- P2：HITL 审批队列 API + 节点流/消息流观测接口。

