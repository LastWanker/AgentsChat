# M0-M2 执行完成记录（V2）

日期：2026-03-11

## 完成范围

- M0 基线冻结：已完成
- M1 协议与状态先行：已完成
- M2 模型层切换 actions[]（含 V1 兼容）：已完成

## 本轮关键产出

## M0

- 执行：
  - `python _GraphChat/scripts/export_graph_viewer.py --no-open`
  - `pytest -c _GraphChat/pytest.ini _GraphChat/tests`
- 基线记录：`2026-03-11-M0-基线冻结记录.md`

## M1

新增：

- `src/graphchat/domain/models/planning_schema.py`
- `src/graphchat/domain/models/world_command.py`
- `tests/unit/test_action_plan_schema.py`
- `tests/unit/test_world_command_schema.py`

更新：

- `src/graphchat/application/state.py`（新增 V2 TTL/生命周期/多动作字段，保留 V1 兼容字段并注释）
- `src/graphchat/domain/models/schemas.py`（新增 `validate_action_plan_v2`、`validate_world_command_payload`）
- `src/graphchat/application/runtime.py`（V2 默认参数：`ttl_initial=15`、`lastlife_threshold=3`、`speak_reply_window_seconds=10`；`test_mode` 下 1s）

## M2

更新：

- `src/graphchat/infrastructure/llm/model_provider.py`
  - 主输出改为 V2：`actions[]` + `task_done`
  - 保留 V1 兼容：`action/payload`
  - DeepSeek 结构化输出 schema 切换到 `ActionPlanOutput`
  - 提示词加入：最多 6 步、动作可重复、默认优先级与 step_index 覆盖规则
- `src/graphchat/application/nodes/agent_nodes.py`
  - 优先读取 V2 `actions[]`
  - 兼容回退到 V1 `action/payload`

新增测试：

- `tests/unit/test_model_provider_actions_plan.py`

## 验证结果

```powershell
pytest -c _GraphChat/pytest.ini _GraphChat/tests
```

- 结果：`24 passed in 1.12s`

## 兼容策略执行情况

- 已明确并落地 `V2主字段` / `V1兼容字段` 注释。
- 主路径已迁移到 `actions[]`，旧字段仍保留供现有图节点使用。
